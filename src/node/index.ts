import { Paragraph } from './paragraph';
import { Blockquote } from './blockquote';
import { Heading } from './heading';
import { Image } from './image';
import { Hr } from './hr';

export const nodes = [new Paragraph(), new Blockquote(), new Heading(), new Hr(), new Image()];
