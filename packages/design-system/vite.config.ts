/* Copyright 2021, Milkdown by Mirone. */

import { defineConfig } from 'vite';

import { viteBuild } from '../../vite.config';

export default defineConfig({
    build: viteBuild('design-system'),
});
