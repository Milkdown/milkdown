---
'@milkdown/crepe': minor
---

Export the `aiProviderConfig` and `aiInstructionTooltipAPI` ctx slices from `@milkdown/crepe/feature/ai`, so a host rendering its own toolbar can gate an AI button on a configured provider and open the instruction palette.
